package main

// metadata: "data that provides information about other data", but not the content of the data itself
// data that will be returned after a user upload a file: {upload date, file name, size, type(pdf, csv, xlxs,...)}

func main() {

	// var db = connect()
	// defer db.Close()
	// router := gin.Default()
	// router.GET("/")
}
